# ???? WEB ????
server:
  port: 7090
  servlet:
    #    context-path: /api
    # session???? 60M ????
    session:
      # Spring Boot 3.x ???? Duration ???PT60M ?? 60 ??
      timeout: PT60M


# ????
security:
  bcrypt:
    rounds: 8
# jwt ????
jwt:
  access-secret: "your-very-long-and-secure-access-key-at-least-32-characters"  # ??32???
  refresh-secret: "your-very-long-and-secure-refresh-key-at-least-32-characters"  # ??32???
  access-expiration: 7200          # 2?? = 2 * 60 * 60 ?
  refresh-expiration: 604800       # 7? = 7 * 24 * 60 * 60 ?
  max-expiration: 2592000          # 30? = 30 * 24 * 60 * 60 ?

spring:
  #  ????
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 10MB

  # ??favicon
  mvc:
    static-path-pattern: /**
    favicon:
      enable: false
    # ????
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: true
      static-locations: classpath:/META-INF/resources/


  # ?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/easypan?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    hikari: # HikariCP ?????
      pool-name: HikariCPDatasource
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: "SELECT 1" # ???????????

  # ????????
  mail:
    host: smtp.qq.com # ?????????? smtp.qq.com
    port: 465 # ???????????465?587?
    username: 859249687@qq.com # ???????
    password: odgtoxjxlvfzbbgd # ??????? (????????????????????QQ??)
    default-encoding: UTF-8 # ??????
    properties:
      mail.smtp.socketFactory.class: javax.net.ssl.SSLSocketFactory # SSL ???? (javax.net.ssl ?Java????????jakarta)
      mail.debug: true # ?? debug????????????????
  # ??????

  # Spring redis??
  data:
    redis:
      database: 0 # Redis?????????0?
      host: 127.0.0.1
      port: 6379
      timeout: 2000ms # ???????????????????YAML Duration???
      jedis:
        pool:
          max-active: 20 # ????????????????????
          max-wait: -1ms # ????????????????????????????????YAML Duration???
          max-idle: 10 # ???????????
          min-idle: 0 # ???????????

# ????
project:
  folder: D:/work/code/myProject/CloudDriver/Backend-Java/easypan

# ?????? (??????? Logback ??)
log:
  root:
    level: debug

# ?????id
admin:
  emails: 859249687@qq.com

# ???????
dev: false

## qq?? ##
qq:
  app:
    id: 12333
    key: 2222222
  url:
    authorization: "https://graph.qq.com/oauth2.0/authorize?response_type=code&client_id=%s&redirect_uri=%s&state=%s"
    access-token: "https://graph.qq.com/oauth2.0/token?grant_type=authorization_code&client_id=%s&client_secret=%s&code=%s&redirect_uri=%s"
    openid: "https://graph.qq.com/oauth2.0/me?access_token=%S"
    user-info: "https://graph.qq.com/user/get_user_info?access_token=%s&oauth_consumer_key=%s&openid=%s"
    redirect: http://easypan.wuhancoder.com/qqlogincalback


# springdoc-openapi????
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.chunfeng.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn

# mybatis
mybatis:
  configuration:
    map-underscore-to-camel-case: true